/*
 * problem.h - Function Declarations
 * 
 * WHAT TO EDIT:
 * - Change the return type and parameters of solution() to match the problem
 * - Add any helper function declarations if needed
 * - Add includes if you need specific types (e.g., #include <vector>)
 * 
 * WHAT NOT TO EDIT:
 * - The #ifndef guard structure
 * - Keep the function name as "solution()" for consistency
 * 
 * EXAMPLES:
 * - void solution();                    // No input/output, prints directly
 * - int solution(int n);                // Takes input, returns result
 * - vector<int> solution(vector<int>);  // Array problems
 * 
 * WORKFLOW:
 * 1. Read the HackerRank problem
 * 2. Determine what solution() should accept and return
 * 3. Update this declaration
 * 4. Implement in problem.cpp
 */

#ifndef PROBLEM_H
#define PROBLEM_H

#include <vector>
#include <string>

// EDIT THIS: Change return type and parameters based on problem requirements
int solution();

#endif
